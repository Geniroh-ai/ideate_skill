---
description: Build a McKinsey-style pitch deck through a guided wizard — context → design → markdown → HTML → PDF
---

Invoke the `ideate` skill and follow its wizard exactly. Begin from Step 0 (resolve project location). Ask one question at a time. Wait for the user's reply between steps.
